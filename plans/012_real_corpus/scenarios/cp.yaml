# Generated by `kerfsim import` from /Users/gb/github/harmonik/docs/decompose-to-tasks/cp-pilot-data.yaml.
#
# NOTE: agent_model.duration and bead_arrivals.generator carry
# defaults only. They should be tuned per Plan 012 B-step3 fitted
# distributions before drawing scoring conclusions.
# pilot cp: dropped 123 edge(s) to unknown pilot prefix

seed: 42
ticks: 20000
agents: 4
works:
  - codename: cp
    areas:
      - invariant
      - req
      - schema
      - test-infra
    deps: []
    bead_count: 85
bead_arrivals:
  generator:
    rework_rate_per_tick: 0.001
    target_works:
      - cp
agent_model:
  duration:
    kind: from_distribution
    distribution: task_work
  spin_up:
    kind: from_distribution
    distribution: spin_up
merge_model:
  base_duration:
    kind: from_distribution
    distribution: merge
  conflict_probability: 0.04
  conflict_duration:
    kind: from_distribution
    distribution: conflict_resolution
