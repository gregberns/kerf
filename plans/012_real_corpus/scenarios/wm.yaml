# Generated by `kerfsim import` from /Users/gb/github/harmonik/docs/decompose-to-tasks/wm-pilot-data.yaml.
#
# NOTE: agent_model.duration and bead_arrivals.generator carry
# defaults only. They should be tuned per Plan 012 B-step3 fitted
# distributions before drawing scoring conclusions.
# pilot wm: dropped 58 edge(s) to unknown pilot prefix

seed: 42
ticks: 20000
agents: 4
works:
  - codename: wm
    areas:
      - error-taxonomy
      - invariant
      - req
      - schema
      - test-infra
    deps: []
    bead_count: 71
bead_arrivals:
  generator:
    rework_rate_per_tick: 0.001
    target_works:
      - wm
agent_model:
  duration:
    kind: from_distribution
    distribution: task_work
  spin_up:
    kind: from_distribution
    distribution: spin_up
merge_model:
  base_duration:
    kind: from_distribution
    distribution: merge
  conflict_probability: 0.04
  conflict_duration:
    kind: from_distribution
    distribution: conflict_resolution
