# Go binary
kerf

.omc/
.env
.claude/settings.local.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Session state and local agent tooling
HANDOFF.md
.codex/
.ntm/
.vscode/

# Local coverage profiles (CI uploads as artifact; never commit).
coverage.out
